import { sendSupport } from '../actions';
export default async function Contact({searchParams}:{searchParams:Promise<{enviado?:string}>}){const q=await searchParams;return <div className="panel" style={{maxWidth:650}}><span className="eyebrow">Estamos aqui para ajudar</span><h1>Fale com a equipe</h1><p className="muted">Se o frete não pôde ser calculado, conte o CEP, a peça e o nome da artesã. A equipe pode orientar você sobre as opções de entrega.</p>{q.enviado&&<div className="notice success">Mensagem recebida. A equipe responderá pelo e-mail informado.</div>}<form action={sendSupport} className="card-box stack"><label>Nome<input className="field" name="name" required maxLength={100}/></label><label>E-mail<input className="field" name="email" type="email" required maxLength={200}/></label><label>Mensagem<textarea className="field" name="message" required maxLength={2000}/></label><input name="website" tabIndex={-1} autoComplete="off" style={{position:'absolute',left:-9999}}/><button className="button">Enviar mensagem</button></form></div>}
