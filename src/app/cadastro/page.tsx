import { signUp } from '../actions';
import Link from 'next/link';
export default async function Signup({searchParams}:{searchParams:Promise<{erro?:string}>}){const q=await searchParams;return <div className="panel" style={{maxWidth:560}}><span className="eyebrow">Faça parte</span><h1>Crie sua conta</h1><p className="muted">Compradoras e artesãs começam pelo mesmo cadastro. Depois, a artesã completa seu perfil no painel.</p>{q.erro&&<p className="notice">Não foi possível criar a conta. Confira os dados.</p>}<form action={signUp} className="card-box stack"><label>E-mail<input className="field" name="email" type="email" required/></label><label>Senha<input className="field" name="password" type="password" minLength={8} required/></label><button className="button">Criar conta</button><p>Já possui conta? <Link className="text-link" href="/entrar">Entrar</Link></p></form></div>}
