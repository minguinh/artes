import { signIn } from '../actions';
import Link from 'next/link';
export default async function Login({searchParams}:{searchParams:Promise<{erro?:string;novo?:string}>}){const q=await searchParams;return <div className="panel" style={{maxWidth:560}}><span className="eyebrow">Bem-vinda de volta</span><h1>Entrar</h1>{q.erro&&<p className="notice">Confira e-mail e senha.</p>}{q.novo&&<p className="notice success">Conta criada. Se a confirmação de e-mail estiver ativa, confirme antes de entrar.</p>}<form action={signIn} className="card-box stack"><label>E-mail<input className="field" name="email" type="email" required/></label><label>Senha<input className="field" name="password" type="password" required/></label><button className="button">Entrar</button><p>Nova por aqui? <Link className="text-link" href="/cadastro">Criar conta</Link></p></form></div>}
